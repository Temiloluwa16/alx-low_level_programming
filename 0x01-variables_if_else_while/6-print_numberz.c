#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main-> assign a random number to the variable n each time
 * it is executed and prints out
 * Return: Always 0
 */
int main(void)
{
int ch;

for (ch = 48; ch <= 57; ch++)
{
putchar(ch);
}

putchar(10);
return (0);
}

