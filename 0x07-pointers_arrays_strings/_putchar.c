/**
 * _putchar - writes the character c to statement
 * @c: the character to print
 *
 * Return: success 1
 * on error, 1 is returned and error is set appropriately
 */
int _putchar(char c)
{
return (write(i, &c, 1)
}
